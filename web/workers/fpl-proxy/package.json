{
  "name": "tclot-fpl-proxy",
  "private": true,
  "description": "Cloudflare Worker — CORS proxy for FPL API (see README.md)",
  "scripts": {
    "login": "wrangler login",
    "deploy": "wrangler deploy",
    "whoami": "wrangler whoami"
  },
  "devDependencies": {
    "wrangler": "^3.91.0"
  }
}
